services:
  postgres:
    image: postgres:17-alpine
    container_name: vbodlaci-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: vbodlaci
      POSTGRES_USER: vbodlaci
      POSTGRES_PASSWORD: vbodlaci_dev_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U vbodlaci -d vbodlaci"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
